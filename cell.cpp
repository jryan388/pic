#include <vector>
#include "vector3d.h"
#include <string>
#include <iostream>

Cell::Cell(double originX_, double originY_, vector< vector< poss_ > >, vector< vector< vels_ > >) {
  originX = originX_;
  originY = originY_;
  poss = poss_;
  vels = vels_;
}
